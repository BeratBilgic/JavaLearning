package com.example.effectivejava.Item21;

public class Item21 {
    public static void main(String[] args){
        /*
        In the presence of default methods, existing implementations of an interface may compile without error or
        warning but fail at runtime. While not ter- ribly common, this problem is not an isolated incident either.
         */

        /*
        In short, avoid relying too much on default methods in interfaces,
        and refrain from adding to existing interfaces without careful consideration. Even when using default methods,
        approach interface creation thoughtfully, assuming that many programmers will implement it.
         */
    }
}
